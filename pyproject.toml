[project]
name = "vitis-ai-porting-advisor"
version = "0.1.0"
description = "AI-powered pipeline that analyses deep learning models for Xilinx DPU hardware compatibility and generates porting strategies."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
